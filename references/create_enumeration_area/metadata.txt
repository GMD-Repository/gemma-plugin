; ---------------------------------------------------------------------------
; Create Enumeration Areas — QGIS Plugin Metadata
; ---------------------------------------------------------------------------
; Required by the QGIS Plugin Manager and Processing registry.
; See: https://docs.qgis.org/3.40/en/docs/pyqgis_developer_cookbook/plugins/plugins.html#plugin-metadata

[general]
name=Create Enumeration Areas
qgisMinimumVersion=3.40
qgisMaximumVersion=3.99
description=Delineates and merges Enumeration Areas (EAs) based on household-count thresholds, with optional road/river boundary snapping.
about=This plugin provides the "Create Enumeration Areas" Processing algorithm under the GMD Toolkits provider. It splits over-populated EAs using weighted K-Means + Voronoi spatial partitioning, and merges under-populated EAs with their most suitable neighbors, while respecting barangay-nesting constraints.

version=1.0.0
author=<CPA>
email=<gmdsupport@psa.gov.ph>

; Changelog (newest first)
changelog=
    1.0.0 - Initial release. Packaged CreateEAAlgorithm as an installable QGIS plugin.

; Tags shown in the Plugin Manager
tags=enumeration area, delineation, census, household, spatial analysis, processing

; Optional links — fill in if publishing to the QGIS Plugin Repository
homepage=
tracker=
repository=

; Plugin type — set to yes because this plugin registers a Processing provider
hasProcessingProvider=yes

; Set to True to be listed in the experimental plugins tab
experimental=False

; Set to True if the plugin is deprecated (do not use)
deprecated=False
